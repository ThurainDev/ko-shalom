# Example frontend env used as template (commit this file)
# In production you can leave this empty so the app uses same-origin /api
VITE_API_URL=
# Alternate name some tools use - accepted by this app as well
VITE_REACT_APP_BACKEND_BASEURL=
